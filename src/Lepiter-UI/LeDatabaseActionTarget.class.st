"
I am a target for LeDatabase dropdown actions appearing in the hamburger menu of a lepiter database (at left of maximized page).

To use me as a target, define an action as an extension method in LeDatabase, with me as a `target:`.

"
Class {
	#name : #LeDatabaseActionTarget,
	#superclass : #GtPhlowActionUniqueTarget,
	#category : #'Lepiter-UI-Action Targets'
}
